import { Edge } from 'reactflow';
import CustomizableEdge from '@/components/Map/Edges/CustomizableEdge';

export const edgeTypes = {
	customizable: CustomizableEdge,
};

export const initialEdges: Array<Edge> = [
	//   { id: '0', source: '1', target: '2' },
];
